{
  "name": "byron-root",
  "private": true,
  "scripts": {
    "install": "cd front && npm install",
    "build": "cd front && npm run build",
    "start": "cd front && node .output/server/index.mjs"
  }
}
